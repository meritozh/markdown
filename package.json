{
  "name": "markdown",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx tsc -p .",
    "test": "node dist/test/entry.js",
    "retest": "yarn build && yarn test"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^10.5.0",
    "typescript": "^2.9.2"
  }
}
